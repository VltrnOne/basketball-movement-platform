{
  "name": "basketball-movement-platform",
  "version": "1.0.0",
  "description": "Basketball Movement Intelligence Platform",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "install:frontend": "cd frontend && npm install"
  }
}
